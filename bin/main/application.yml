server:
  port: 80

spring:
  application:
    name: assign-service


  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/spring?useSSL=false&useUnicode=false&serverTimezone=Asia/Seoul
    username: root
    password: q2w3e4r5!
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
#spring.jpa.properties.hibernate.format_sql=true